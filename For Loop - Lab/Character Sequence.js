function newRowEverySymbol(input) {

    let name = (input[0]);
    let nameL = name.length;
    for(let index = 0; index < nameL; index++) {
        console.log(name[index])

    }
}
newRowEverySymbol(['gosho']);